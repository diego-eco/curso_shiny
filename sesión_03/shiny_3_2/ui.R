
library(shinydashboard)
library(shiny)

#  Interfaz del usuario (ui) Front End ----

shinyUI(
    dashboardPage(
        dashboardHeader(),
        dashboardSidebar(),
        dashboardBody()
    )
)

